
# NumErrors:      6

proc foo(arg: i32): i64 = {
  printf("foo1: %d\n", i32(arg))
  i64(arg) * i64(arg)
}

proc foo(arg: i64): i64 = {
  printf("foo2: %d\n", i32(arg))
  arg * arg
}

## this should be a doc comment
## spanning multiple lines

proc main():i64 = {
  let tmp: i8 = 123
  foo(tmp) # Error: expected type 'i32 | i64' but got type 'i8'

  let str = "abcdef"
  baz(str, tmp) # Error: proc not found: baz(str, i8)

  let x : i32 = "somestring" # Error: expected type 'i32' but got type 'str'

  let x_ : i32 = 123 # Error: identifier may not start or end with _ (underscore), reserved for internal usage
  let _x : i32 = 321 # Error: identifier may not start or end with _ (underscore), reserved for internal usage
  let x__x : str = "abc" # Error: identifier may not use __ (double underscore), reserved for internal usage

   return 0
}
