#proc assert(cond: Bool)

proc main():i32 = {
  {
    let x:i32 = 1
    let y:i32 = 2
    assert((x <  y) == true)
    assert((x <= y) == true)
    assert((x == y) == false)
    assert((x >= y) == false)
    assert((x >  y) == false)
  }

  {
    let x:f32 = 1.0
    let y:f32 = 2.0
    assert((x <  y) == true)
    assert((x <= y) == true)
    assert((x == y) == false)
    assert((x >= y) == false)
    assert((x >  y) == false)
  }

  {
    let x:f64 = 1.0
    let y:f64 = 2.0
    let z:f64 = 3.0
    printf("abc %f\n", (x + y) * z)
  }
  0
}
