
type MyStruct = struct {
  a: int
  b: float
  c: string
}

proc myfun1(a, b, c: int): int = {
  return a + b * c
}

proc myfun2(a, b, c: int) : int = { a + b * c }

# this is a comment
proc main() : int32 = {
  println("Hallo Welt!")
}
